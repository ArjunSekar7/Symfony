<html>
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; margin-top : 100px }
    
</style>
<body>

<div class="example-wrapper">
    <h1>LoginPage</h1>
        {{ form_start(form) }}
            <h4 > {{ message }} </h4>
            {{ form_widget(form) }}
            <a href="{{path('chart')}}">Chart</a>
            <button class="btn">{{ button_label|default('Login') }}</button>
        {{ form_end(form) }}
</div>


</body>
</html>