<html>
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; margin-top : 100px }
    .form-error-message { color:red; top : 100px; }
    span.form-error-icon {  display: none; }
    #add_user_name{ margin-left: 60px; margin-top:5px;}
    #add_user_email_id { margin-left: 40px; margin-top:5px;}
    #add_user_comments { margin-left: 10px; margin-top:5px;}
    #add_user_country { margin-left: 35px; margin-top:5px; }
    #add_user_submit { margin-left: 350px; margin-top:5px;}
</style>
<body>

<div class="example-wrapper">
    <h1>User Form</h1>
        {% if flag == 1 %}
           <h3 style="color:red;"> Email already Exist </h3>
        {% elseif flag ==2 %}
            <h3 style="color:green;"> Registered Successfully </h3>
        {% else %}
        {% endif %}
        {{ form_start(form) }}  
            {{ form_widget(form) }}
        {{ form_end(form) }}
        <a href={{path('view_user')}}>ViewList</a>
</div>
    
</body>
</html>